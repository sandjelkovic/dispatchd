server:
  port: 8888
spring:
  application:
    name: configserver
  config:
    name: configserver
  boot:
    admin:
      url: http://localhost:8010
logging:
  level:
    com.sandjelkovic: DEBUG
management:
  context-path: /admin
  security:
    enabled: false
