server:
  port: 8888
spring:
  application:
    name: configserver
  config:
    name: configserver
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configs/
#  boot:
#    admin:
#      client:
#        url: "http://localhost:8010"

logging:
  level:
    com.sandjelkovic: DEBUG


