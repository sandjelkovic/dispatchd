server:
  port: 8761
spring:
    application:
        name: eurekaservice
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

#logging:
#  level:
#    com.netflix.eureka: OFF
#    com.netflix.discovery: OFF
